#include "main.h"
/**
 * add - entry point
 *
 * @x: the first integer
 * @y: the second integer
 * Description: using the add function, the program adds two
 * integer and return the result
 * Return: result
 */
int add(int x, int y)
{
	return (x + y);
}

