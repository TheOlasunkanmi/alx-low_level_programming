#include "main.h"
#include <stdio.h>
/**
 * _abs - entry point
 *
 * @n: is the int that will be used as argument
 * Description: using the _abs function, the program
 * will compute the absolute value of n
 * Return: 0
 */
int _abs(int n)
{
	if (n > 0 || n == 0)
	{
		return (n);
	}
	else
		return (c * -1);
}

